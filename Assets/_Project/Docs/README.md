# Prototype Docs

The V0.2 implementation is intentionally dependency-free and uses runtime-generated Unity UI.

Start with `V0.2_GAMEPLAY.md` for mechanics and `ROADMAP.md` for the development sequence.
